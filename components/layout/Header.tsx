"use client";

// Header is now a thin secondary toolbar - replaced by TopBar for primary chrome.
// Retained as a pass-through for backward compat; layout uses AppShell directly.
export function Header() {
  return null;
}
